#include <iostream>
#include <cmath>

bool checkIfSimple(int n) {
    double dn = (double)n;
    double fractPart;
    fractPart = std::modf(std::sqrt(dn), &fractPart);
    if( == 0)
    int r = 2;
}

int main()
{
    std::cout << "Hello World!\n";
}